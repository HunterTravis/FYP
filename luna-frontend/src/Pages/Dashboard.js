// make a dashboard for a university portal
import Sidebar from '../Components/Sidebar';

const Dashboard = () => {
    return (
        <div className="dashboard">
            <Sidebar />
        </div>
    );
}

export default Dashboard;